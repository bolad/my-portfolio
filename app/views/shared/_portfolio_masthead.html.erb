<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">My Portfolio</h1>
    <p class="lead text-muted">This portfolio displays my personal projects</p>
    <p>
      <%= link_to 'Find Out More About Me', about_me_path, class: 'btn btn-primary' %>
      <%= link_to 'My Blog', blogs_path, class: 'btn btn-secondary' %>
      <%= link_to 'Create Portfolio Item', new_portfolio_path, class: 'btn btn-secondary' if logged_in?(:site_admin) %>
    </p>
  </div>
</section>
